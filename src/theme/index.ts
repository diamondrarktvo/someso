import { palette, theme, smsClimTheme, Theme } from "./theme";

import { Size } from "./size";

import { Typography } from "./typography";

export { palette, theme, smsClimTheme, Theme, Size, Typography };

export { useGetTheme } from "./hooks/useGetTheme";
